<!....This is the form for editing a reply.....>
<%= form_for @reply do|f| %>

  <%= f.hidden_field :article_id %>
  <%= f.hidden_field :user_id %>
   <%= f.hidden_field :comment_id %>
   
    <%= f.text_area :des %>
    
    <%= f.submit "Add Reply" %>
  <% end %>
